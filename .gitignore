/target
.worktrees/
.demo/
.superpowers/
ddns.db*
demo/demo.db*
demo/cookies.txt
demo/client*.log
demo/client*.err.log
# dx bundle web output (generated by `dx bundle --platform web`)
dist/
# deploy/.env holds production secrets — copy from .env.example (git has no
# inline comments, so the pattern must be the bare path on its own line)
deploy/.env
# Local Pi runtime state
.atl/
.reasonix/
tunnello-vps/deploy/.env

# Agent skill tooling (npx skills)
.agents/
.claude/
skills-lock.json
